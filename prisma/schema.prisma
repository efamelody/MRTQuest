// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Better Auth tables will be managed by Better Auth
// This schema is primarily used to establish the Prisma client connection to Supabase
// Better Auth will handle creating: user, session, account, verification tables
